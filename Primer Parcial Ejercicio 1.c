#include stdio.h
    int a;
    void eleccion();
    void factorial();
    
    void main(){
        eleccion();
        int c;
         c=a;
        factorial();
        printf(El factorial de %i es %i,c,a);
    return 0;
    }

    eleccion(){
        printf(por favor, ingresa un numero del 1 al 5 );
        scan    f(%i,&a);
        if (a=5 && a=1){
        return a;
        }
        else
        printf(El numero ingresado no es valido, intenta de nuevo n);
        eleccion();
    }

    factorial(){
        int b;
        b = a;
        while (b1){
        b = b-1;
        a= ab;
        }
        return a;
    }